// Top-level build file where you can add configuration options common to all sub-projects/modules.

ext {
  ffmpegKitPackage = "https"
  ffmpegKitReleaseTag = "ffmpeg-android-6.0.2"
}

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
    // FFmpeg AARs from the react-native-ffmpeg-kit fork's GitHub Releases.
    // Must be here (visible to :app) so :app can resolve the transitive @aar.
    ivy {
      url "https://github.com/2004durgesh/ffmpeg-kit/releases/download"
      patternLayout {
        artifact "[revision]/[artifact].[ext]"
      }
      metadataSources { artifact() }
      content { includeGroup "com.arthenica.ffmpegkit" }
    }
  }
}

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
